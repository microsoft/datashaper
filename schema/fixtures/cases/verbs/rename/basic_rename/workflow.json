{
	"$schema": "../../../../../workflow.json",
	"steps": [
		{
			"verb": "rename",
			"input": "table7",
			"args": {
				"columns": {
					"ID": "uuid",
					"item": "product",
					"quantity": "amount",
					"totalSale": "total"
				}
			},
			"id": "expected"
		}
	],
	"output": ["expected"],
	"input": ["table7"]
}
