{
	"$schema": "../../../../../workflow.json",
	"steps": [
		{
			"verb": "rename",
			"input": "table7",
			"args": {
				"columns": {
					"ID": "uuid",
					"item": "product",
					"quantity": "amount",
					"totalSale": "total"
				}
			},
			"output": "expected"
		}
	]
}
