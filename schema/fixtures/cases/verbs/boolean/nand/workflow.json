{
	"$schema": "https://microsoft.github.io/datashaper/schema/workflow.json",
	"steps": [
		{
			"verb": "boolean",
			"input": "table20",
			"args": {
				"to": "newColumn",
				"columns": ["A", "B", "C"],
				"operator": "nand"
			},
			"id": "expected"
		}
	],
	"output": ["expected"],
	"input": ["table20"]
}
