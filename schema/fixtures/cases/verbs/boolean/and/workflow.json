{
	"$schema": "https://microsoft.github.io/data-wrangling-components/schema/workflow.json",
	"steps": [
		{
			"verb": "boolean",
			"input": "table30",
			"args": {
				"to": "newColumn",
				"columns": ["A", "B", "C"],
				"operator": "and"
			},
			"id": "expected"
		}
	],
	"output": ["expected"],
	"input": ["table30"]
}
