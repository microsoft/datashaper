{
	"$schema": "../../../../workflow.json",
	"steps": [
		{
			"verb": "boolean",
			"input": "table20",
			"args": {
				"to": "newColumn",
				"columns": ["A", "B", "C"],
				"operator": "and"
			},
			"output": "expected"
		}
	]
}
