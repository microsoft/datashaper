{
	"$schema": "https://microsoft.github.io/data-wrangling-components/schema/workflow.json",
	"steps": [
		{
			"id": "expected",
			"verb": "unroll",
			"input": "table1",
			"args": {
				"columns": ["ID"]
			}
		}
	],
	"input": ["table1"],
	"output": ["expected"]
}
