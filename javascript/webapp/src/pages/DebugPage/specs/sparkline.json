{
	"$schema": "../../../../../../schema/workflow.json",
	"name": "Sparkline",
	"description": "Aggregates a daily prices into an array to demonstrate Sparkline cells",
	"steps": [
		{
			"id": "n1",
			"verb": "aggregate",
			"inputs": { "default": { "node": "data/stocks.csv" } },
			"outputs": { "default": "sparkline" },
			"args": {
				"groupby": "Symbol",
				"column": "Close",
				"operation": "array_agg",
				"to": "Daily"
			}
		}
	]
}
