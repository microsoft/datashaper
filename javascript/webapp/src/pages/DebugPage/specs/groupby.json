{
	"$schema": "../../../../../../schema/workflow.json",
	"name": "Group by",
	"description": "",
	"steps": [
		{
			"id": "n1",
			"verb": "groupby",
			"inputs": { "default": { "node": "data/stocks.csv" } },
			"outputs": { "default": "groupby-2" },
			"args": {
				"columns": ["Symbol", "Month"]
			}
		}
	]
}
